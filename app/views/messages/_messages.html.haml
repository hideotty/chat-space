- @group.messages.each do |message|
  .message__user
    = message.user.name
    %span.message__date
      = message.created_at.strftime('%Y/%m/%d/ %H:%M:%S')
  .message__text
    = message.content
    =image_tag message.img_url if message.img.present?
